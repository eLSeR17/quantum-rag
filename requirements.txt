# QuantumRAG — root dependency pins for CI security scanning (pip-audit).
# Deployable deps live in demo_ui/requirements.txt.
# Core runtime pins (matching pyproject.toml):
chromadb>=0.4.22,<2.0
# sentence-transformers>=5.5 floors transformers to the 5.x line;
# transformers>=5.10 clears PYSEC-2026-2288/2289/2290/3929 (fixed in 5.3/5.5/5.10).
sentence-transformers>=5.5,<6.0
transformers>=5.10,<6.0
streamlit>=1.41,<2.0
pandas>=2.0
numpy>=1.24,<2.0
