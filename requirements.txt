# QuantumRAG — root dependency pins for CI security scanning (pip-audit).
# Deployable deps live in demo_ui/requirements.txt.
# Core runtime pins (matching pyproject.toml):
chromadb>=0.4.22,<1.0
sentence-transformers>=2.7,<4.0
streamlit>=1.41,<2.0
pandas>=2.0
numpy>=1.24,<2.0
